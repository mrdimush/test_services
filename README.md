1.0 - added localhost stub for the vulnerable service 
writing the first enumerator for the password
default username is "skillbox"
password list in hardcoded inside

using the requests standard library